---
- name: install docker packages
  package:
    name: "{{ item }}"
    state: installed
  with_items:
    - docker
    - docker-compose

- name: start docker service
  service:
    name: docker
    state: started
    enabled: yes
